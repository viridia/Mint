# -----------------------------------------------------------------------------
# Mintfile for building RE2.
# -----------------------------------------------------------------------------

include_dirs = [
  '.'
]

re2 = library {
  sources = glob("re2/*.cc")
  outputs = [ 're2.a' ]
}
